{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "watch": "node scripts/auto-build.js",
    "dev:auto": "concurrently \"npm run watch\" \"npm run preview\"",
    "dev:live": "vitepress dev docs",
    "build:manual": "node scripts/manual-build.js",
    "build:force": "touch .build-trigger",
    "pull:build": "git pull && npm run build:manual",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "chokidar": "^4.0.3",
    "concurrently": "^9.2.0",
    "glob": "^11.0.3",
    "nodemon": "^3.1.10",
    "vitepress": "^1.6.3"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "mermaid": "^11.8.0",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
